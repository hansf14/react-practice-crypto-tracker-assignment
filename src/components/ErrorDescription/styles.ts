import styled from "styled-components";
import { ErrorDescriptionBaseElementTypeValue } from "./types";

export const ErrorDescription = styled(ErrorDescriptionBaseElementTypeValue)`
	text-align: center;
	display: block;
`;
